apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nedu-local-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: nginx
    targetRevision: "*"
    helm: {}
  destination:
    server: https://kubernetes.default.svc
    namespace: gitops-local
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
